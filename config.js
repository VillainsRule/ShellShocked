export default {
    // for development, exposes some variables to the console
    // easily picked up by potential anticheat code, only enable this if you know what you're doing
    exposeVariables: false,

    // adds a unique identifier to each version
    devBuildID: false,

    // set the version
    version: '2.0.1'
};